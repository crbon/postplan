# Do not copy this file to configure a Convex deployment.
# From the repo root, select the R2 project's existing development deployment:
#   pnpm exec convex deployment select <r2-project-dev-deployment>
# Convex writes .env.local for this checkout. Keep that file untracked.
# Set application secrets on the project's production deployment with:
#   pnpm exec convex env set --prod <NAME>
