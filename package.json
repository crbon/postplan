{
  "name": "postplan",
  "version": "0.4.0",
  "description": "Publish a self-contained HTML document with Convex and private Cloudflare R2 storage.",
  "private": true,
  "type": "module",
  "license": "MIT",
  "files": [
    "bin",
    "src",
    "LICENSE",
    "README.md"
  ],
  "bin": {
    "postplan": "./bin/postplan.js"
  },
  "scripts": {
    "deploy": "convex deploy",
    "logs": "convex logs --prod",
    "test": "node --import tsx --test test/*.test.ts",
    "test:r2": "node --import tsx --test test/r2.integration.ts",
    "test:e2e": "node --import tsx --test test/e2e.integration.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "commander": "^14.0.0",
    "nanoid": "^5.1.5",
    "parse5": "^7.3.0"
  },
  "devDependencies": {
    "convex": "^1.43.0",
    "tsx": "^4.23.15"
  }
}
